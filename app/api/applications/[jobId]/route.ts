import { z } from "zod";
import { updateApplicationRecord } from "@/lib/repositories/applications";
const nullableDate=z.union([z.literal(""),z.string().date()]).nullable();const schema=z.object({dateApplied:nullableDate,followupDate:nullableDate,applicationUrl:z.union([z.literal(""),z.string().url().max(2000)]).nullable(),submissionDeadline:nullableDate,notes:z.string().max(10000).nullable(),outcome:z.string().max(200).nullable(),rejectionStage:z.string().max(200).nullable(),offerDetails:z.string().max(5000).nullable()});
export async function PATCH(request:Request,{params}:{params:Promise<{jobId:string}>}){try{const parsed=schema.safeParse(await request.json());if(!parsed.success)return Response.json({error:"Check the dates and application URL."},{status:400});const {jobId}=await params;await updateApplicationRecord(jobId,parsed.data);return Response.json({ok:true})}catch(error){return Response.json({error:error instanceof Error?error.message:"Could not save application."},{status:500})}}
