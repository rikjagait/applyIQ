import { getJob } from "@/lib/repositories/jobs";
import { buildApplicationStudio } from "@/lib/application-studio";
function escape(value:string){return value.replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;")}
export async function GET(_request:Request,{params}:{params:Promise<{jobId:string}>}){const {jobId}=await params;const job=await getJob(jobId);if(!job)return new Response("Job not found",{status:404});const studio=buildApplicationStudio(job);const html=`<!doctype html><html><head><meta charset="utf-8"><title>${escape(job.title)} application pack</title><style>body{font:11pt Arial;line-height:1.5;max-width:760px;margin:40px auto;color:#17211f}h1{font-size:22pt}h2{margin-top:28px;border-bottom:1px solid #ccc;padding-bottom:5px}.meta{color:#60706b}.page{page-break-before:always}li{margin:7px 0}</style></head><body><h1>Neelam Jagait</h1><p class="meta">Tailored application pack · ${escape(job.title)} · ${escape(job.company)}</p><h2>Professional summary</h2><p>${escape(studio.summary)}</p><h2>Core skills</h2><p>${studio.skills.map(escape).join(" · ")}</p><h2>Selected experience</h2><ul>${studio.changes.map(c=>`<li>${escape(c.tailored)}</li>`).join("")}</ul><div class="page"><h1>Cover letter</h1>${studio.coverLetter.split("\n").map(p=>`<p>${escape(p)}</p>`).join("")}<h2>Application answers</h2>${studio.answers.map(a=>`<h3>${escape(a.question)}</h3><p>${escape(a.answer)}</p>`).join("")}</div></body></html>`;return new Response(html,{headers:{"content-type":"text/html; charset=utf-8","content-disposition":`attachment; filename="${job.company.replace(/[^a-z0-9]+/gi,"-").toLowerCase()}-application-pack.html"`}})}
