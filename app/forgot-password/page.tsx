import { redirect } from "next/navigation";
import { isSupabaseConfigured } from "@/lib/supabase/config";
export default async function ForgotPasswordPage({searchParams}:{searchParams:Promise<{error?:string}>}){if(!isSupabaseConfigured())redirect("/");const {error}=await searchParams;return <div className="login-shell"><div className="login-card"><div className="brand" style={{color:"var(--ink)",padding:0,marginBottom:28}}><span className="brand-mark">A</span>ApplyIQ</div><div className="eyebrow">Account access</div><h1>Create or reset password</h1><p className="subtle">We’ll email Neelam a secure one-time link. The account password is never visible to ApplyIQ.</p>{error&&<div className="error-box" role="alert">{error}</div>}<form className="form" action="/auth/recover" method="post"><label className="field">Email<input className="input" name="email" type="email" autoComplete="email" required defaultValue="neelam.c@hotmail.co.uk"/></label><button className="btn primary" type="submit">Send secure password link</button><a className="btn" href="/login">Back to sign in</a></form></div></div>}
