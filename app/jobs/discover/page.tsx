import Link from "next/link";
import { ArrowUpRight, Link2, Search, Sparkles } from "lucide-react";
import { JobDiscovery } from "@/components/job-discovery";
import { JobRow, PageHead } from "@/components/ui";
import { listJobFeeds, listDailyDiscoveries } from "@/lib/repositories/job-feeds";
import { listJobs } from "@/lib/repositories/jobs";
import { getJobPreferences } from "@/lib/repositories/preferences";

function linkedInJobs(role:string,location:string){const params=new URLSearchParams({keywords:role,location,f_TPR:"r604800"});return `https://www.linkedin.com/jobs/search/?${params}`}
export default async function DiscoverJobsPage(){const [feeds,saved,discovered,preferences]=await Promise.all([listJobFeeds(),listJobs(),listDailyDiscoveries(),getJobPreferences()]);const recommended=saved.filter(job=>job.score>=65).sort((a,b)=>b.score-a.score).slice(0,5);const fresh=discovered.filter(job=>job.isNew).slice(0,5);const linkedinRoles=preferences.roles.slice(0,5).map(role=>/manager|director|lead/i.test(role)?role:`${role} Manager`);const linkedinLocation=preferences.locations[0]||"New York";return <div className="content"><PageHead eyebrow="Step 1 · One place to find work" title="Find jobs" copy="Start with ApplyIQ’s best opportunities, search LinkedIn, check a company, or paste a role you already found."/><section className="find-job-choices" aria-label="Ways to find a job"><a href="#today"><span>1</span><Sparkles size={18}/><strong>Today’s opportunities</strong><small>Review the strongest matches first</small></a><a href="#linkedin"><span>2</span><Search size={18}/><strong>Search LinkedIn</strong><small>Open focused searches from preferences</small></a><a href="#paste-job"><span>3</span><Link2 size={18}/><strong>Paste a job URL</strong><small>Analyze a specific vacancy</small></a></section><section className="section" id="today"><div className="section-head"><div><div className="eyebrow">Recommended for Neelam today</div><h2>Your best current opportunities</h2><p className="subtle">Strongest matches first. Low-fit roles stay out of the way.</p></div><span className="pill">{recommended.length+fresh.length} to review</span></div><div className="stack find-today-list">{recommended.map(job=><JobRow key={job.id} job={job}/>)}{fresh.map(job=><article className="card opportunity" key={`${job.provider}-${job.externalId}`}><div><span className="pill">New today · {job.matchScore}</span><h3>{job.title}</h3><div className="job-company">{job.company} · {job.location}</div><p className="job-reason">{job.matchReason}</p></div><Link className="btn primary" href={`/jobs/new?url=${encodeURIComponent(job.jobUrl)}`}>Review role <ArrowUpRight size={13}/></Link></article>)}{recommended.length+fresh.length===0?<div className="card empty private-empty-jobs"><h2>Your private opportunity list is ready to fill</h2><p className="subtle">Demo roles are not copied into the private workspace. Start with one of the focused LinkedIn searches below, then paste a promising role into ApplyIQ for automatic analysis.</p><a className="btn primary" href="#linkedin">Search LinkedIn jobs</a></div>:null}</div></section><section className="section linkedin-source" id="linkedin"><div className="section-head"><div><div className="eyebrow">LinkedIn · user-controlled search</div><h2>Recent LinkedIn opportunities</h2><p className="subtle">These searches use Neelam’s saved roles and {linkedinLocation} location, limited to the past week.</p></div><Link className="btn" href="/settings">Change preferences</Link></div><div className="linkedin-searches">{linkedinRoles.map(role=><a className="card" href={linkedInJobs(role,linkedinLocation)} target="_blank" rel="noreferrer" key={role}><span className="pill">LinkedIn Jobs</span><strong>{role}</strong><small>{linkedinLocation} · posted in the last week</small><span>Open search <ArrowUpRight size={13}/></span></a>)}</div><div className="analysis-banner linkedin-note">ApplyIQ does not scrape or automate LinkedIn. Open a search, choose a role, copy its URL, and paste it below. This protects Neelam’s LinkedIn account while keeping the workflow simple.</div></section><section className="section find-method" id="company-search"><div className="section-head"><div><div className="eyebrow">Direct employer sourcing</div><h2>Search company career sites</h2></div></div><JobDiscovery initialFeeds={feeds}/></section><section className="section card paste-job-card" id="paste-job"><div><div className="eyebrow">LinkedIn or any job site</div><h2>Paste the job-posting link</h2><p className="subtle">ApplyIQ automatically imports the details, assesses the match and shows clear next actions.</p></div><form action="/jobs/new" method="get"><input className="input" aria-label="Job posting URL" name="url" type="url" placeholder="https://www.linkedin.com/jobs/view/…" required/><button className="btn primary" type="submit">Analyze job URL <ArrowUpRight size={14}/></button></form></section></div>}
