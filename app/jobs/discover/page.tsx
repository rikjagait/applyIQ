import Link from "next/link";
import { ArrowUpRight, Link2, Search, Sparkles } from "lucide-react";
import { JobDiscovery } from "@/components/job-discovery";
import { JobRow, PageHead } from "@/components/ui";
import { listJobFeeds, listDailyDiscoveries } from "@/lib/repositories/job-feeds";
import { listJobs } from "@/lib/repositories/jobs";

export default async function DiscoverJobsPage(){const [feeds,saved,discovered]=await Promise.all([listJobFeeds(),listJobs(),listDailyDiscoveries()]);const recommended=saved.filter(job=>job.score>=65).sort((a,b)=>b.score-a.score).slice(0,5);const fresh=discovered.filter(job=>job.isNew).slice(0,5);return <div className="content"><PageHead eyebrow="Step 1 · One place to find work" title="Find jobs" copy="Start with ApplyIQ’s best opportunities for today, search a company, or paste a role you already found."/><section className="find-job-choices" aria-label="Ways to find a job"><a href="#today"><span>1</span><Sparkles size={18}/><strong>Today’s opportunities</strong><small>Review the strongest matches first</small></a><a href="#company-search"><span>2</span><Search size={18}/><strong>Search a company</strong><small>Check live employer career sites</small></a><a href="#paste-job"><span>3</span><Link2 size={18}/><strong>Paste a job URL</strong><small>Analyze a specific vacancy</small></a></section><section className="section" id="today"><div className="section-head"><div><div className="eyebrow">Recommended for Neelam today</div><h2>Your best current opportunities</h2><p className="subtle">Strongest matches first. Low-fit roles stay out of the way.</p></div><span className="pill">{recommended.length+fresh.length} to review</span></div><div className="stack find-today-list">{recommended.map(job=><JobRow key={job.id} job={job}/>)}{fresh.map(job=><article className="card opportunity" key={`${job.provider}-${job.externalId}`}><div><span className="pill">New today · {job.matchScore}</span><h3>{job.title}</h3><div className="job-company">{job.company} · {job.location}</div><p className="job-reason">{job.matchReason}</p></div><Link className="btn primary" href={`/jobs/new?url=${encodeURIComponent(job.jobUrl)}`}>Review role <ArrowUpRight size={13}/></Link></article>)}{recommended.length+fresh.length===0?<div className="card empty"><h2>No strong matches waiting today</h2><p className="subtle">Try a company search below. ApplyIQ will keep weak roles filtered out.</p></div>:null}</div></section><section className="section find-method" id="company-search"><div className="section-head"><div><div className="eyebrow">Find more opportunities</div><h2>Search company career sites</h2></div></div><JobDiscovery initialFeeds={feeds}/></section><section className="section card paste-job-card" id="paste-job"><div><div className="eyebrow">Already found a role?</div><h2>Paste the job-posting link</h2><p className="subtle">ApplyIQ will import the details, assess the match and show clear next actions.</p></div><form action="/jobs/new" method="get"><input className="input" aria-label="Job posting URL" name="url" type="url" placeholder="https://company.com/careers/job…" required/><button className="btn primary" type="submit">Analyze job URL <ArrowUpRight size={14}/></button></form></section></div>}
