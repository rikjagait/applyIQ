import type { DiscoveredJob } from "@/lib/job-discovery/ats";
const roleSignals=["learning","training","engagement","community","program","project","partnership","stakeholder","events","education","enablement","people","talent","operations","customer success"];
const targetLocations=["new york","nyc","jersey city","new jersey","remote","united states","us -"];
export function discoveryPriority(job:DiscoveredJob,preferences?:{roles?:string[];locations?:string[]}){const title=job.title.toLowerCase();const location=job.location.toLowerCase();const roles=preferences?.roles?.length?preferences.roles.map(x=>x.toLowerCase()):roleSignals;const locations=preferences?.locations?.length?preferences.locations.map(x=>x.toLowerCase()):targetLocations;const roleHits=roles.filter(signal=>title.includes(signal)).length;const locationFit=locations.some(target=>location.includes(target));const excessiveSeniority=/\b(chief|vice president|vp|head of)\b/.test(title);const technicalMismatch=/\b(software|engineer|scientist|architect|security|accounting|counsel|hardware)\b/.test(title)&&roleHits===0;return roleHits*30+(locationFit?25:0)-(excessiveSeniority?25:0)-(technicalMismatch?50:0)}
export function prioritizeDiscoveredJobs(jobs:DiscoveredJob[],limit=50,preferences?:{roles?:string[];locations?:string[]}){return jobs.map(job=>({job,priority:discoveryPriority(job,preferences)})).filter(item=>item.priority>=45).sort((a,b)=>b.priority-a.priority||((b.job.postedAt?Date.parse(b.job.postedAt):0)-(a.job.postedAt?Date.parse(a.job.postedAt):0))).slice(0,limit).map(item=>item.job)}
