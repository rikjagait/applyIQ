"use client";
import { useState } from "react";
import { UploadCloud } from "lucide-react";
export function ResumeUpload(){const [name,setName]=useState("");const [file,setFile]=useState<File|null>(null);const [pending,setPending]=useState(false);const [message,setMessage]=useState("");async function upload(){if(!file)return;setPending(true);setMessage("");const body=new FormData();body.set("resume",file);try{const response=await fetch("/api/resumes/upload",{method:"POST",body});const result=await response.json();if(!response.ok)throw new Error(result.error);setMessage(`Stored privately and extracted ${result.characters.toLocaleString()} characters.`);}catch(error){setMessage(error instanceof Error?error.message:"Upload failed");}finally{setPending(false);}}return <div className="upload"><UploadCloud size={30} color="var(--green)"/><h3 style={{marginTop:12}}>Upload a new master résumé</h3><p className="subtle">DOCX or PDF · maximum 10 MB · private storage only</p><div className="actions" style={{justifyContent:"center"}}><label className="btn" style={{cursor:"pointer"}}>Choose file<input hidden type="file" accept=".docx,.pdf" onChange={e=>{const selected=e.target.files?.[0]??null;setFile(selected);setName(selected?.name??"");setMessage("");}}/></label>{file&&<button className="btn primary" onClick={upload} disabled={pending}>{pending?"Extracting securely…":"Store as master"}</button>}</div>{name&&<p className="truth">Selected: {name}</p>}{message&&<p className="analysis-banner" role="status">{message}</p>}</div>}
