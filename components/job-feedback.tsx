"use client";
import { useState } from "react";
export function JobFeedback({jobId}:{jobId:string}){const [open,setOpen]=useState(false);const [pending,setPending]=useState(false);const [message,setMessage]=useState("");async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();setPending(true);const values=Object.fromEntries(new FormData(event.currentTarget));const response=await fetch(`/api/jobs/${jobId}/feedback`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(values)});const result=await response.json() as {error?:string};setMessage(response.ok?"Feedback saved. ApplyIQ will use this context in future ranking.":result.error||"Could not save feedback.");setPending(false);if(response.ok)setOpen(false)}return <div className="feedback-wrap"><button className="btn danger" onClick={()=>setOpen(value=>!value)}>Dismiss / feedback</button>{open&&<form className="form status-popover" onSubmit={submit}><label className="field">Feedback type<select className="input" name="feedbackType"><option value="dismissed">Dismiss opportunity</option><option value="not_useful">Not useful</option><option value="useful">Useful recommendation</option></select></label><label className="field">Primary reason<select className="input" name="reason"><option value="role_mismatch">Role mismatch</option><option value="location">Location</option><option value="compensation">Compensation</option><option value="seniority">Seniority</option><option value="industry">Industry</option><option value="requirements">Missing requirements</option><option value="duplicate">Duplicate</option><option value="not_interested">Not interested</option><option value="good_recommendation">Good recommendation</option></select></label><label className="field">Notes<textarea className="input" name="notes" style={{minHeight:70}}/></label><button className="btn primary" disabled={pending}>{pending?"Saving…":"Save feedback"}</button></form>}{message&&<p className="subtle">{message}</p>}</div>}
