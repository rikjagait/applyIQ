"use client";
import { useState } from "react";
import { Sparkles } from "lucide-react";
import { CopyMessage } from "@/components/copy-message";
export function CustomApplicationQuestion({jobId}:{jobId:string}){const [answer,setAnswer]=useState("");const [error,setError]=useState("");const [pending,setPending]=useState(false);async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();setPending(true);setError("");setAnswer("");const question=String(new FormData(event.currentTarget).get("question")||"");const response=await fetch(`/api/applications/studio/${jobId}/answer`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({question})});const result=await response.json() as {answer?:string;error?:string};if(!response.ok)setError(result.error||"Could not draft an answer.");else setAnswer(result.answer||"");setPending(false)}return <section className="card"><div className="section-head"><div><div className="eyebrow">Custom question</div><h2>Draft another answer</h2></div><Sparkles color="var(--green)"/></div><form className="form" onSubmit={submit}><label className="field">Paste the employer’s exact question<textarea className="input" name="question" placeholder="For example: Tell us about a complex program you managed…" required minLength={5}/></label>{error&&<div className="error-box">{error}</div>}<button className="btn primary" disabled={pending}>{pending?"Drafting from Career Truth…":"Generate factual answer"}</button></form>{answer&&<div style={{marginTop:16}}><div className="section-head"><h3>Draft answer</h3><CopyMessage text={answer}/></div><p className="message-draft">{answer}</p></div>}</section>}
