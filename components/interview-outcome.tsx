"use client";
import { useState } from "react";
import { CopyMessage } from "@/components/copy-message";
export function InterviewOutcome({id,company,role,interviewer,initialOutcome,initialNotes}:{id:string;company:string;role:string;interviewer:string|null;initialOutcome:string|null;initialNotes:string|null}){const [message,setMessage]=useState("");const [pending,setPending]=useState(false);const thankYou=`Hi ${interviewer||"[Name]"},\n\nThank you for taking the time to speak with me about the ${role} opportunity at ${company}. I appreciated learning more about the role and the team’s priorities. Our conversation reinforced my interest in the opportunity and the relevance of my experience leading programs, building stakeholder partnerships and delivering measurable engagement outcomes.\n\nPlease let me know if I can provide any additional information.\n\nBest,\nNeelam`;async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();setPending(true);const values=Object.fromEntries(new FormData(event.currentTarget));const response=await fetch(`/api/interviews/${id}`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify(values)});const result=await response.json() as {error?:string};setMessage(response.ok?"Interview outcome saved.":result.error||"Could not save outcome.");setPending(false)}return <div className="grid two-col section"><section className="card"><div className="eyebrow">Record outcome</div><form className="form" onSubmit={submit}><label className="field">Outcome<select className="input" name="outcome" defaultValue={initialOutcome||"Pending"}><option>Pending</option><option>Progressing</option><option>Rejected</option><option>Offer</option><option>Withdrawn</option></select></label><label className="field">Interview notes<textarea className="input" name="notes" defaultValue={initialNotes||""}/></label>{message&&<div className={message.includes("saved")?"analysis-banner":"error-box"}>{message}</div>}<button className="btn primary" disabled={pending}>{pending?"Saving…":"Save outcome"}</button></form></section><section className="card"><div className="section-head"><div><div className="eyebrow">Thank-you message</div><h2>Reviewed draft</h2></div><CopyMessage text={thankYou}/></div><p className="message-draft">{thankYou}</p></section></div>}
